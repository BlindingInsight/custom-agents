{
  "name": "demo-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that proxies *.summitaigroup.com to Tailscale Funnel",
  "devDependencies": {
    "wrangler": "^3"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  }
}
